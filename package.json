{
  "name": "ts-cli",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "age": "ts-node src/index.ts age",
    "calc": "ts-node src/index.ts calc -o",
    "hello": "ts-node src/index.ts hello",
    "build": "npx tsc",
    "clean": "rm -rf dist",
    "js-cli": "npm run build && node dist/src/index.js",
    "test": "mocha --require ts-node/register test/**/*.ts",
    "ts-cli-installts-cli": "npm run build && npm install -g",
    "ts-cli-uninstall": "npm uninstall -g ts-cli",
    "ts-cli-run": "ts-cli hello Globally Insalled CLI in TypeScript"
  },
  "bin": {
    "ts-cli": "./dist/src/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.3.6",
    "@types/mocha": "^10.0.1",
    "@types/node": "^20.6.1",
    "chai": "^4.3.8",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "chalk": "^5.3.0",
    "commander": "^11.0.0",
    "npm-check-updates": "^16.14.3"
  }
}
